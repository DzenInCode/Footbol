# Football Manager (Spring Boot) — early version

University coursework, 2019. Spring Boot 1.5 + Spring Data JPA + PostgreSQL + Lombok + Maven + Docker.

A more polished REST API version: [Man](https://github.com/DzenInCode/Man).

**Status:** archived learning artifact, not maintained.
